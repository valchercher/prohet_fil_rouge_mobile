import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ODC_FR_Academy',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
