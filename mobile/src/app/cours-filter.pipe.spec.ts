import { CoursFilterPipe } from './cours-filter.pipe';

describe('CoursFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CoursFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
